# Core orchestrator
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
httpx>=0.27
python-dotenv>=1.0

# Reasoning (Nemotron via NIM is OpenAI-compatible)
openai>=1.30

# Voice agent — pinned to match the canonical reference repo
# (pipecat-ai/nemotron-january-2026). Imports verified empirically against this
# exact version; do not float it. Extras we actually use:
#   pip install "pipecat-ai[silero,openai,deepgram,elevenlabs,daily,runner,webrtc]==0.0.98"
pipecat-ai[silero,openai,deepgram,elevenlabs,daily,runner,webrtc]==0.0.98
